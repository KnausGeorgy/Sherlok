<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.knaus_g.sherlok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScreenTwo"
            android:label="Choise">
            <!--<intent-filter>
                <action android:name="ru.knaus_g.sherlok.ScreenTwo" />
                <category android:name="android.intent.categoru.DEFAULT" />
                </intent-filter>-->
        </activity>
        <activity android:name=".Camera_movie">
        </activity>

        <activity android:name=".About_programmer">
        </activity>
    </application>

</manifest>